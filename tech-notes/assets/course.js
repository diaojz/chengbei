const esc=(v)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function roadmap(data){document.querySelector('#weeks').innerHTML=data.weeks.map(w=>`<article class="syllabus-row"><span>W${String(w.number).padStart(2,'0')}</span><div><h3>${esc(w.title)}</h3><p>${esc(w.outcome)}</p></div><b>${esc(w.project)}</b></article>`).join('')}
function lessons(data){const target=document.querySelector('#lesson-list');const render=(filter='all')=>{const rows=data.lessons.filter(x=>filter==='all'||x.direction===filter);target.innerHTML=rows.length?rows.map(x=>`<a class="visual-card" href="${esc(x.href)}"><img src="assets/images/${esc(x.image||'structured-output.png')}" alt="${esc(x.imageAlt||'课程主题配图')}"><div><span>${esc(x.date)} · ${esc(x.direction)} · ${esc(x.duration)} 分钟</span><h3>${esc(x.title)}</h3><p>${esc(x.summary)}</p><b>阅读完整课程 →</b></div></a>`).join(''):'<p class="empty">这个方向还没有已发布课程，先从完整体系查看学习顺序。</p>'};document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)}))}
function library(data){document.querySelector('#knowledge-list').innerHTML=data.knowledgeBase.map(x=>`<a class="knowledge-link" href="${esc(x.href)}"><span>${esc(x.count)} 条</span><b>${esc(x.title)}</b><i>↗</i></a>`).join('')}
fetch('data/season-1.json').then(r=>{if(!r.ok)throw new Error(`课程数据加载失败：${r.status}`);return r.json()}).then(data=>{roadmap(data);lessons(data);library(data)}).catch(error=>{console.error(error);document.querySelectorAll('#weeks,#knowledge-list').forEach(x=>x.innerHTML='<p class="empty">课程数据暂时没有加载成功，请稍后刷新。</p>')});

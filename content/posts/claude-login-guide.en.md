# Claude Login · Full Guide

*Chinese version is the source of truth — this English version is a stub. See the [Chinese version](#/p/claude-login-guide) for the full guide.*

Most students get stuck at "I can't log in to Claude." This page collects every common failure point and the matching fix.

**TL;DR for non-Chinese readers:** use the relay gateway at `https://gw.chengbei.org` — register an account, get an API key, plug it into Claude Code via `ANTHROPIC_BASE_URL` and `ANTHROPIC_API_KEY`. Skip the official Claude account headache.

For the detailed guide, common pitfalls, and a comparison of all three routes (relay / official account / built-in IDE), please read the Chinese version above.
